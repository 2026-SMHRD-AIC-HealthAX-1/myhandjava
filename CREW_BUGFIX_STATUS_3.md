# 크루 기능 3개 버그 수정 상태

1. 멤버 응답에 크루장이 빠진 구형 API도 `leader`/`leaderId`로 보완하며, 모든 멤버 화면에 `크루장` 뱃지를 표시합니다. 본인과 크루장은 강퇴·권한 변경 대상에서 제외합니다.
2. 가입 신청은 `PENDING` 상태로 전송·저장하도록 계약을 고정하고, 조회 결과에서도 PENDING만 렌더링합니다. 페이지 재진입·승인/거절·WebSocket 이벤트 시 목록을 다시 조회합니다.
3. 크루 컨셉은 `concepts: string[]`로 생성·수정·상세·목록 전체 경로에서 전달하고, 카드와 상세 화면에서 배열 전체를 렌더링합니다.

`backend-patch/`는 첨부 ZIP에 백엔드 원본이 없어 별도로 제공하는 Spring Boot/JPA 적용 코드입니다.
